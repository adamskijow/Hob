<!-- SPDX-License-Identifier: MIT -->
# Hob 1.0 acceptance audit

This is the release-candidate evidence matrix for Hob 1.0. It is intentionally
started before v0.9 releases so missing proof cannot be reconstructed from
memory at the end. A green unit suite is evidence for the behavior a test names,
not proof of the broader customer journey.

Current baseline: v0.9.12 targets 452 deterministic Python tests, 40 native
tests across the Store foundation and Open Local service controller, a 112-case
real-model evaluation plus an end-to-end analysis gate, signed EventKit build,
and Ubuntu/macOS CI. The live v0.8 execution journey and v0.9.1
conversational-retraction replay
completed on 2026-07-11 with clean durable queues. Sustained v0.9 outlook and
EOD use remains. Mac App Store distribution is now an explicit parallel track;
its architecture decision and first native package exist, but no distributable
Store app is claimed yet.

## Release decision

**Not a 1.0 release candidate.** The product has strong local correctness and
privacy boundaries, but evidence is missing for sustained execution-loop use,
fresh-install timezone correctness, recoverable permanent queue failures, DST
edges, accessible end-to-end operation, supported-model quality, and a fully
rehearsed upgrade/rollback journey. v0.9.11 added one-command durable service
installation plus a native menu-bar recovery surface, but clean-install,
logout/reboot, and rollback rehearsals remain. Daily-use evidence must still
show that the planning loop reduces work rather than merely adding output.

## Evidence matrix

| Area | Current evidence | Assessment | Evidence required for 1.0 |
| --- | --- | --- | --- |
| User onboarding | Private owner pairing, resumable five-step work profile, natural keep-current/pause answers, `/settings`, one-time upgrade discovery, first-adoption coaching, one-command durable Mac install, and native menu-bar recovery | Partial. Manual plist editing and post-reboot uncertainty are removed, but chat setup does not confirm timezone, bot creation/token entry still leaves Terminal in the Open Local journey, and a fresh-machine rehearsal is missing. | Fresh macOS rehearsal from clone to paired, reboot-safe daemon with correct local timezone, denied/authorized Calendar branches, model readiness, first task, first digest decision, first plan, adoption, and recovery performed without developer help. |
| Capture | Text, forwarded text, edits, captions, multi-capture, multi-completion, contextual zero-completion recap reports, numbered exclusions, dates, deadlines, priority, tags, notes, waiting, dependencies, recurrence, undo | Strong deterministic coverage. Free-form scope, tense, recurrence, retraction, and contextual decisions are model-owned typed intent; core validation bounds ids, prompt provenance, values, and effects without language-specific repairs. Typed `this` and `next` weekday meanings remain distinct in deterministic calendar resolution. Far-future holds concisely name the affected work, exact date, and unusual distance before offering an explicitly labeled confirmation. Every active EOD reply receives an independent reasoning-first semantic pass. Morning nudges, confirmations, and onboarding also separate answer relation from message intent, inspect the first typed proposal, require answer evidence, and cannot replace a different concrete action. Unsupported owner media receives a text fallback, and Telegram-generated status events are durable silent no-ops. | Keep expanding the real-model corpus with noisy forwards, partial progress, unsupported-media UX, adversarial context collisions, and regional date-language expectations. No acknowledged message may disappear, create or complete the wrong task, and no platform service event may impersonate owner input. |
| Planning | Deterministic daily feasibility, explicit work profile, Calendar busy time, proposals, adoption/replacement, weekly outlook, grounded explanation, temporary what-if proposals | v0.9.9 stores bounded deterministic facts, lets the model select only known ids, and reruns feasibility on cloned task state. Durable corrections and adoption remain explicit. | Release and dogfood the why/what-if loop; verify clarity, latency, stale-result recovery, and that no hypothetical changes durable task/settings state. |
| Calendar behavior | Signed read-only EventKit bridge emits only busy ranges; denial and absence fall back | Privacy boundary is strong. Permission and timezone/DST journeys are not release-proven. | Manual grant, denial, revocation, all-day, overlapping, timezone, spring-forward, fall-back, bridge-missing, and bridge-timeout checks. Output must state when Calendar was not used. |
| Reminders | Global lead, per-task offsets, snooze, missed-window suppression, reply anchoring, durable outbox, adopted-session nudge | A live adopted-session nudge and direct reply passed with clean queues and `started`, not false completion, state. DST and permanent Telegram failure remain uncovered. | Timed reminder and nudge interaction across restart/sleep; DST tests; permanent-send recovery that cannot block later delivery forever. |
| Recurrence | Daily, weekdays, multiple weekdays, monthly day, yearly date, intervals, fixed/completion anchors, skips, stop/end/count, undo/migration | Broad deterministic coverage. Natural-language support claims are wider than the real-model corpus. | Real-model cases for monthly/yearly/multiple-weekday/end/skip/stop, month-end/leap-year matrix, edit/undo/restart rehearsal, and no silent one-off fallback for a recognized recurrence request. |
| Recovery | Atomic turns, durable inbox/outbox, dedupe, restart, migration backup, verified restore/import, database lease | Strong crash/retry foundation. Semantic uncertainty is no longer mislabeled as an outage and cannot retry forever; a live pending turn survived 12 attempts and then applied/delivered exactly once. A permanently failing transport/model inbox or outbox row can still remain at the head, and there is no safe operator quarantine command. | Privacy-safe queue inspection plus explicit retry/quarantine/recover operations, failed-outbox accounting, true poison-message tests, disk-full/corrupt-candidate drills, and a live backup/restore/rollback rehearsal. |
| Accessibility | Primary interface is text; buttons have text labels and equivalent plain-language actions; long replies split safely; media fallback is textual; menu-bar state has text, symbol, and color plus a `hobctl` fallback | Partial. There is no explicit VoiceOver audit, command-menu registration, date/time comprehension audit, or proof that every Telegram flow works without reactions/buttons/pins. | VoiceOver/manual Telegram and menu-bar pass, keyboard/text-only completion of every journey, meaningful button labels, no emoji/color-only state, clear human dates plus exact dates where needed, caption behavior, unsupported-media feedback, and command discoverability. |
| Failure states | Model outages preserve inbox work; Calendar failure degrades; send failures remain durable; status/doctor expose health | Partial. Doctor does not validate Telegram credentials, poison queues have no terminal recovery, and log/disk exhaustion is not exercised. | Invalid/revoked token, Ollama unavailable/slow/malformed, Telegram offline/permanent rejection, Calendar timeout, database busy/corrupt/full, restart during each boundary, and actionable operator output for every case. |
| Upgrades | Released-schema fixtures, automatic pre-migration backup, portable plan-session export, once-version discovery, repeatable Mac install/update/repair preserving the daemon plist and previous companion | Strong data migration and an initial productized service update path. End-to-end rollback is not rehearsed. Runtime and package version drift is test-blocked. | Clean v0.8 to v0.9 to v1.0 rehearsal using copies of live data, verified backups, rollback, release-note delivery once, legacy-profile preservation, daemon/menu restart, and post-upgrade health/interaction checks. |
| Privacy and safety | Local model, single owner, private-chat enforcement, Keychain token, read-only title-free Calendar edge, explicit plan consent, privacy-safe status | Strong by design, pending RC threat-model replay. | Verify logs, status, doctor, exports, backups, errors, prompts, and bridge output for tokens/event text/unnecessary identifiers; unauthorized user/group/callback/reaction probes; document retention and deletion expectations. |
| Operational reliability | launchd/Hearth deployment, one-command Mac service/menu install, visible running state, restart, explicit deep health, queue counts, schema integrity, backup/restore, CI | Partial. Installation and basic operator recovery are productized, but logout/reboot/update/rollback are not yet rehearsed, logs have no managed rotation, and queues/history have no sustained-size evidence. A 20-second Hearth deep probe falsely restarted direct Ollama traffic during long 14B generations three times. Daily use also proved that overlapping proactive contexts could create a deterministic retry loop; morning/EOD precedence and retry classification are now regression-tested and live-replayed. | Validate durable install/update/rollback end to end, make monitoring distinguish in-flight work from a wedge, run reboot/sleep tests, set a log policy, measure database growth/latency, observe queue drain, and complete seven days without manual process repair. |
| Mac App Store edition | Accepted two-edition architecture, Xcode-built SwiftUI shell, embedded login-item helper and Foundation Models tool, explicit `SMAppService` lifecycle, real-generation readiness gate, first shared-fixture native task slice, private App Group task/undo state, typed-turn inbox/outbox, v1-to-v2 migration, and confirmed previous-copy recovery in Settings | Foundation plus local transaction seed. Basic capture/correction/date/hold/undo semantics cross-check both implementations and survive restart; interrupted and duplicate typed turns apply once; ordered compact reply receipts survive restart; corruption and failed persistence cannot become an empty list or false acknowledgement; onboarding exposes content-free storage health. Runtime activation stays locked. The build remains unsigned in CI and lacks Telegram update receipt/rendering, full behavior parity, pairing, privacy manifest, manual accessibility evidence, submission archive, and App Review build. | One distribution-signed self-contained sandboxed bundle; successful model probe on eligible and ineligible states; end-to-end durable Telegram-update/interpretation/turn/outbox pipeline; complete shared parity and Apple-model corpora; no-Terminal onboarding and recovery; archive/sandbox/privacy/accessibility/review rehearsals; successful App Store review before calling the Store edition 1.0. |
| LLM-native differentiation | Natural capture/correction, semantic contextual outcomes, semantic recall, typed planning, explicit replan, grounded why/change selection, and temporary what-if proposals | Strong implementation evidence. Free-form language reaches the model; deterministic facts, ids, bounds, cloned state, invalidation, and adoption confine effects. The remaining question is customer value and latency in sustained use. | Dogfood must show the loop reduces planning work versus a conventional scheduler, and every declared model must pass explanation/durable/counterfactual contrasts. |
| Sustained daily use | Morning digest delivered and ordinary interaction tested; privacy-safe activation metrics exist | Missing for execution and outlook loops. | At least seven consecutive real days spanning capture, correction, digest, reminder, adopted plan, interruption/replan/replacement, nudge, EOD, weekly outlook, model outage or restart, and one backup. Record counts/timestamps and qualitative friction without storing private text in the audit. |

## Prioritized findings

### Resolved P0 parent gate: the v0.8 execution loop works live

The live database now contains the deliberately exercised v0.8 journey: initial
proposal, explicit adoption, status lookup, natural interruption, a separate
proposal while the old plan stayed active, explicit replacement, durable start
nudge, and direct reply anchored as `started`. Final aggregate state was one
active and one superseded run, three canceled old sessions, one started and two
planned revised sessions, with clean inbox/outbox queues. This closes the v0.8
parent gate. v0.9 still requires release and seven days of outlook/EOD daily use.

### P1: first-run time can be wrong by default

`HOB_TIMEZONE` defaults to UTC, while chat setup asks hours and days without
confirming timezone. A new macOS owner can finish setup successfully and receive
digests, plans, and reminders at the wrong local time. 1.0 must derive the
system timezone safely or make confirmation an unavoidable onboarding step,
with the result visible and changeable.

### P1: one poison delivery can stop the product indefinitely

Inbox and outbox processing is ordered and stops on the first failure, which is
correct for transient faults. A permanent parsing/store/Telegram failure is
never quarantined, so every later turn or reply can remain behind it forever.
Status shows pending work but offers no privacy-safe inspect/retry/quarantine
operation. Recovery must preserve at-most-once mutations and make any discard a
conscious local operator action.

### Resolved implementation P1: background recovery is product-visible

v0.9.11 makes `scripts/install_macos.sh` the repeatable Open Local install,
update, and repair path. It preserves existing daemon configuration, refuses an
ambiguous database, installs automatic background startup, and adds a native
menu-bar teapot with running state, restart, privacy-safe health, logs, and data
folder access. `scripts/setup.sh` invokes it and `scripts/hobctl` provides the
same recovery concepts without a graphical session. A clean-machine,
logout/reboot, update, rollback, Calendar-authorization, and uninstall rehearsal
is still required before the broader 1.0 journey is closed.

### P1: DST behavior is unproven

Planning constructs timezone-aware wall times, but no tests cover nonexistent
spring-forward times or duplicated fall-back times. Reminders, fixed tasks,
Calendar periods, adopted sessions, and day boundaries need an explicit policy
and deterministic tests.

### P1: the supported-model promise is not evidence-backed

The runtime default and documentation offer `qwen2.5:7b-instruct`, while the
release eval gate runs only the 14B model. Either the supported baseline must
pass the complete corpus on target hardware or 1.0 must make 14B the declared
requirement and preflight resource expectations honestly.

A 14B audit during the next-weekday fix also produced inconsistent contextual
metadata on two older cases: a waiting-item answer became the correct `resume`
mutation without the nudge decision that clears prompt state, and a request to
finish onboarding later became `skip` rather than `cancel`. The new exact
next-weekday case passed, but 1.0 still needs repeatable full-corpus context
results or a core reconciliation that preserves the correct action and prompt
lifecycle without relying on a second classifier agreeing by chance.

### P1: model supervision can kill legitimate long generations

The live Hearth configuration sends a small-model deep probe every 30 seconds
with a 20-second deadline while Hob talks directly to Ollama. During the
v0.9.10 full gate, a probe queued behind a legitimate 14B request and Hearth
classified the server as stuck, restarting it three times. The identical
111-case gate passed uninterrupted with Hearth paused through its authenticated
control API, then supervision returned healthy. The 1.0 install must either
expose in-flight work to supervision, serialize/defer deep probes correctly, or
set and validate a timeout beyond the supported model's measured tail latency.

### Resolved implementation P1: LLM-native explanation continues into negotiation

v0.9.9 adds typed `explain` and `what_if` queries over a 24-hour deterministic
snapshot. Model output can select only known facts/suggestions; what-ifs clone
tasks, apply bounded temporary inputs, and create only an explicit proposal.
Mutation-shaped counterfactual mistakes receive an independent semantic guard
that fails closed. Live daily-use value and supported-model evidence remain.

### P2: accessible fallback and service-event classification were found early

This audit changed silent non-text Telegram updates into a clear text fallback
and lets media captions use the normal capture path. It also added a test tying
the package version to the runtime version. Daily use then exposed that Hob's
own digest pin emits a non-text Telegram service update, which was incorrectly
given the unsupported-media fallback. v0.9.3 classifies Telegram status updates
as durable silent no-ops while retaining the fallback for actual owner media.
These fixes are prerequisites, not proof of the full accessibility or upgrade
journeys above.

### P2: sustained storage and logs have no operating envelope

Inbox, outbox, action, digest, and plan history are retained indefinitely and
launchd logs are unmanaged by Hob. This may be acceptable for one owner, but
1.0 needs a measured multi-year-size fixture, latency thresholds, documented
retention, and a safe log-rotation recommendation before making that promise.

## Coherent 1.0 scope

1. Finish, release, and dogfood v0.9 without weakening its explicit consent or
   read-only Calendar boundaries.
2. Make first install time-correct and durable: timezone confirmation, complete
   launchd install/update/rollback validation, Telegram credential check, and a
   useful doctor result before the owner expects a digest.
3. Add deterministic queue recovery with privacy-safe status, explicit local
   retry/quarantine commands, failed-outbox accounting, and poison-row tests.
4. Define and test DST policy across planning, Calendar, reminders, recurrence,
   sessions, and digest/EOD scheduling.
5. Dogfood the implemented typed explanation/what-if path. Corrections must
   remain explicit settings, task edits, or new proposals and must never become
   hidden behavioral memory.
6. Close accessibility and discoverability: captions/fallbacks, command menu,
   text-only alternatives, human date/time clarity, and a manual VoiceOver pass.
7. Choose and prove the supported local model baseline, expand the real-model
   corpus for recurrence/explanation/failure language, and document hardware
   expectations.
8. Run clean-install, upgrade, rollback, backup/restore, privacy, failure, soak,
   and seven-day daily-use rehearsals before the 1.0 release candidate.
9. Advance the Mac App Store track without weakening the Open Local edition:
   create the Xcode-owned sandboxed bundle and consented helper, prove shared
   behavioral parity, complete no-Terminal onboarding and accessibility, and
   submit only after the Store-specific gates in `docs/app-store.md` pass.

## Deliberate exclusions for 1.0

- No Calendar writes or automatic task moves.
- No cloud task store, multi-user mode, or web dashboard.
- No behavior-derived productivity scoring, estimate learning, or hidden memory.
- No voice transcription until a local, explicit, resource-bounded privacy
  design exists; unsupported audio receives a clear text alternative.
- No broad autonomous messaging beyond the owner-approved digest, recap,
  reminders, adopted-session nudges, and once-version discovery.

## Final release-candidate gates

- No P0 or P1 finding above remains open, and every claimed fix has a named
  deterministic regression plus the appropriate manual/live evidence.
- Full Python suite and compile check pass from a clean environment; the exact
  count is recorded rather than treated as the quality claim.
- Complete real-model corpus passes on every declared supported model.
- Signed EventKit bridge builds and its grant/deny/revoke/privacy matrix passes.
- Ubuntu and macOS CI pass on the exact release commit and tag.
- Fresh install, v0.8/v0.9 upgrade, rollback, backup, restore, restart, sleep,
  DST, permanent queue failure, and unauthorized-access rehearsals pass.
- Seven-day privacy-safe daily-use evidence covers the complete loop and records
  qualitative usability findings and their disposition.
- Release version, package metadata, docs, handoff, schema, backup, tag, GitHub
  release, deployed process, and status all agree on 1.0.0.
- A final promise audit finds no claim that exceeds tested behavior, no known
  critical defect, and no misleading implication that planned, elapsed, or
  forecast work is complete or that Hob writes Calendar data.

## Evidence log

The v0.8 execution-loop dogfood completed on 2026-07-11 with privacy-safe live
aggregate evidence recorded in `docs/audits/v0.8.md`. Pending v0.9
merge/release/deployment and seven-day use, plus implementation of the coherent
1.0 scope.
